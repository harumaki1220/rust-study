fn main() {
    // Rustは型を推論してくれますが、明示することもできます

    // --- 数値 (Scalar Types) ---

    // 整数 (Integer)
    // :u32 は「32ビット符号なし整数」を明示しています
    let unsigned_int: u32 = 42_949; // '_' は桁区切りで見やすさのため。無視されます
    println!("符号なし整数: {}", unsigned_int);

    // :i32 は「32ビット符号あり整数」 (デフォルト)
    let signed_int: i32 = -100;
    println!("符号あり整数: {}", signed_int);

    // 浮動小数点数 (Float)
    // :f64 (64ビット) がデフォルト
    let float_64 = 3.14159; 
    println!("浮動小数点数 (f64): {}", float_64);

    // --- 真偽値 (Boolean) ---
    let is_rust_fun: bool = true;
    println!("Rustは楽しい？ {}", is_rust_fun);

    // --- 文字 (Character) ---
    // 'char' はシングルクォートで囲みます。
    // 日本語や絵文字もOK (4バイトのUnicode)
    let character = 'A';
    let emoji = '🦀'; // Rustのマスコット、フェリス君
    println!("文字: {}, 絵文字: {}", character, emoji);
}